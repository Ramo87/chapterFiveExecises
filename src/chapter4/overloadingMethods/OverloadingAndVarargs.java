package chapter4.overloadingMethods;

/**
 * Created by Loky on 05/08/2020.
 */
public class OverloadingAndVarargs {

    /** public void fly(int[] lengths) { }
    public void fly(int... lengths) { } // DOES NOT COMPILE  - - - -> Remember that Java treats varargs as if they were an array.
     This means that the method signature is the same for both methods. */
}
